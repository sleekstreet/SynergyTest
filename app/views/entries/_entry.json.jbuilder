json.extract! entry, :id, :firstName, :lastName, :address, :apt, :city, :state, :email, :purchTime, :interest, :possPurch, :opinion, :partcipate, :minor, :sign, :news, :dealerCont, :time, :created_at, :updated_at
json.url entry_url(entry, format: :json)
